<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="webservice.list">
      <field name="name">webservice list</field>
      <field name="model">webservice.webservice</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="webservice.action_window">
      <field name="name">webservice window</field>
      <field name="res_model">webservice.webservice</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="webservice.action_server">
      <field name="name">webservice server</field>
      <field name="model_id" ref="model_webservice_webservice"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="webservice" id="webservice.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="webservice.menu_1" parent="webservice.menu_root"/>
    <menuitem name="Menu 2" id="webservice.menu_2" parent="webservice.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="webservice.menu_1_list" parent="webservice.menu_1"
              action="webservice.action_window"/>
    <menuitem name="Server to list" id="webservice" parent="webservice.menu_2"
              action="webservice.action_server"/>
    -->
  </data>
</odoo>