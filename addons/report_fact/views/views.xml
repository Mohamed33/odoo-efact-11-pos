<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="report_fact.list">
      <field name="name">report_fact list</field>
      <field name="model">report_fact.report_fact</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="report_fact.action_window">
      <field name="name">report_fact window</field>
      <field name="res_model">report_fact.report_fact</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="report_fact.action_server">
      <field name="name">report_fact server</field>
      <field name="model_id" ref="model_report_fact_report_fact"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="report_fact" id="report_fact.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="report_fact.menu_1" parent="report_fact.menu_root"/>
    <menuitem name="Menu 2" id="report_fact.menu_2" parent="report_fact.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="report_fact.menu_1_list" parent="report_fact.menu_1"
              action="report_fact.action_window"/>
    <menuitem name="Server to list" id="report_fact" parent="report_fact.menu_2"
              action="report_fact.action_server"/>
    -->
  </data>
</odoo>