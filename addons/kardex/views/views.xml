<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="kardex.list">
      <field name="name">kardex list</field>
      <field name="model">kardex.kardex</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="kardex.action_window">
      <field name="name">kardex window</field>
      <field name="res_model">kardex.kardex</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="kardex.action_server">
      <field name="name">kardex server</field>
      <field name="model_id" ref="model_kardex_kardex"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="kardex" id="kardex.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="kardex.menu_1" parent="kardex.menu_root"/>
    <menuitem name="Menu 2" id="kardex.menu_2" parent="kardex.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="kardex.menu_1_list" parent="kardex.menu_1"
              action="kardex.action_window"/>
    <menuitem name="Server to list" id="kardex" parent="kardex.menu_2"
              action="kardex.action_server"/>
    -->
  </data>
</odoo>